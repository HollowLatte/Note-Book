import{_ as e}from"./plugin-vue_export-helper-x3n3nnut.js";import{o as a,c as n,e as s}from"./app-R-jbemKs.js";const t={},i=s(`<h2 id="问题现象" tabindex="-1"><a class="header-anchor" href="#问题现象" aria-hidden="true">#</a> 问题现象</h2><p>Fetch在GitHub的repo时，出现以下错误：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>Fetch Failed github: unable to access &#39;https://github.com/xxx/xxx.git/&#39;: Failed to connect to github.com
port 443 after 21174 ms: Couldn&#39;t connect to server
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="可能原因" tabindex="-1"><a class="header-anchor" href="#可能原因" aria-hidden="true">#</a> 可能原因</h2><p>proxy问题</p><h2 id="原因分析" tabindex="-1"><a class="header-anchor" href="#原因分析" aria-hidden="true">#</a> 原因分析</h2><p>正确开启proxy后，仍会出现该问题，怀疑是git或github问题</p><h2 id="问题原因" tabindex="-1"><a class="header-anchor" href="#问题原因" aria-hidden="true">#</a> 问题原因</h2><p>未知</p><h2 id="解决方法" tabindex="-1"><a class="header-anchor" href="#解决方法" aria-hidden="true">#</a> 解决方法</h2><ol><li><p>开关代理后，刷新dns缓存，使用命令<code>ipconfig /flushdns</code></p></li><li><p>为Git单独进行proxy配置</p></li></ol><p>查看当前安装的Git是否有配置代理：<code>git config -l --global</code></p><p>看显示的Git配置是否有配置如下两项：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token assign-left variable">http.proxy</span><span class="token operator">=</span><span class="token number">127.0</span>.0.1:7890
<span class="token assign-left variable">https.proxy</span><span class="token operator">=</span><span class="token number">127.0</span>.0.1:7890
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>如果有配置这两项，则<strong>检查该配置的IP端口是否与proxy工具</strong>的一致，一般proxy工具会显示proxy端口</p><p>如果git没有上面的配置，则可以通过如下命令进行配置：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> config <span class="token parameter variable">--global</span> http.proxy <span class="token number">127.0</span>.0.1:7890
<span class="token function">git</span> config <span class="token parameter variable">--global</span> https.proxy <span class="token number">127.0</span>.0.1:7890
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>配置完成后刷新下dns，使用命令<code>ipconfig /flushdns</code></p>`,18),r=[i];function o(c,d){return a(),n("div",null,r)}const h=e(t,[["render",o],["__file","cannot-fetch-from-github.html.vue"]]);export{h as default};
